<% if PretestAndExamQuiz.find(params[:id]).errors.any? %>
    <div id="error_explanation">
        <div class="alert alert-danger">
            The form contains <%= pluralize(PretestAndExamQuiz.find(params[:id]).errors.count, "error") %>.
        </div>
        <ul>
        <% PretestAndExamQuiz.find(params[:id]).errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
        </ul>
    </div>
<% end %>
